/* batch.html*/
.detail-text {
    // padding: 10px;
    // margin: 10px;
    display: inline;
}
.btn_group:before {
  content: "."; 
  display: block; 
  height: 0; 
  font-size:0;	
  clear: both; 
  visibility:hidden;
}
.btn_group {
    display: block;
}
.batch-html .action:nth-child(odd) {
    background-color: rgba(230, 230, 230, 0.3);
}
.action {
    // border-top: 2px dotted #ddd;
    // margin-top: 10px;
    padding: 15px 10px;
}